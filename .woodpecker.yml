pipeline:
  build:
    image: alpine:latest
    commands:
      - apk add --no-cache make poetry
      - poetry install
      - poetry shell
      - make build
  publish:
    image: alpine:latest
    commands:
      - echo "Testing.."