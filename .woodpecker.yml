pipeline:
  build:
    image: alpine:latest
    commands:
      - apk add --no-cache make python-pip
      - pip install staticjinja
      - make build
  publish:
    image: alpine:latest
    commands:
      - echo "Testing.."